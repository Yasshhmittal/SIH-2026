# PRAHARI backend dependencies
# Python 3.11+ required. Everything here runs locally with no cloud services.

# --- web ---
fastapi>=0.115
uvicorn[standard]>=0.32
sse-starlette>=2.1
httpx>=0.27

# --- contracts / config ---
pydantic>=2.9
PyYAML>=6.0

# --- deterministic maths (never the LLM) ---
sympy>=1.13
pint>=0.24

# --- deliverables ---
python-docx>=1.1
openpyxl>=3.1
python-pptx>=1.0

# --- documents ---
pymupdf>=1.24

# --- tests ---
pytest>=8.0

# --- not yet wired (P3-P6); uncomment as those phases land ---
# qdrant-client>=1.12          # knowledge plane
# rapidocr-onnxruntime>=1.3    # OCR cascade, no system binary needed
